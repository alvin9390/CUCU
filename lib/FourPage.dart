import 'package:flutter/material.dart';

class FourPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('차용증 판매하기'),
      ),
      body: Center(
        child: Text('This is the Four Page'),
      ),
    );
  }
}
